<item>
  <title>{{ message.title }}</title>
  <link>{{ message.url | absolute_url }}</link>
  <description>
    Audio recording of {{ site.author }}'s message on {{ message.date | date: "%B %d, %Y" }}.
  </description>
  <enclosure url="{{ message.audio | absolute_url }}" length="{{ message.audio_length }}" type="audio/mpeg" />
  <pubDate>{{ message.date | date: "%a, %d %b %Y %H:%M:%S EST" }}</pubDate>
  <guid>{{ message.url | absolute_url }}</guid>
</item>
